"use client";

import { ArrowLeft, ArrowRight, Check, ChevronDown, CircleAlert, CircleCheck, CloudLightning, Database, Gift, KeyRound, Layers3, Loader2, Orbit, PlugZap, RefreshCw, Search, ShieldCheck, Timer, UserRound, X } from "lucide-react";
import { useCallback, useEffect, useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import Image from "next/image";

type Choice = { id: string; name: string; currency?: string; status?: string; type?: string };
type Overview = { connected: boolean; authorizedAt?: string; businessCenters: Choice[]; advertisers: Choice[]; warnings: string[] };
type Detail = { advertiser: Choice | null; pixels: Choice[]; identities: Choice[]; warnings: string[] };
type Notice = { tone: "success" | "warning" | "error"; text: string };

const currencies = ["BRL", "USD", "MXN", "CLP", "COP", "PEN", "ARS"];
const steps = ["CONECTAR", "CONTAS", "CATÁLOGO", "CRIATIVOS", "ESTRUTURA", "PROXY", "LANÇAR"] as const;
const stageCopy = [["Forjando nova rota.", "Autorize o canal para montar sua operação."], ["Contas de anúncio", "Selecione as contas que deseja gerenciar."], ["Catálogo", "Selecione um catálogo por Business Center."], ["Adicionar criativos", "Configure vídeo, texto do anúncio, CTA e complementos interativos."], ["Estrutura ABO", "Defina volume, orçamento, público e nomenclatura."], ["Rota de saída", "Defina o caminho de rede da operação."], ["Preparar lançamento", "Revise cada ativo antes de enviar ao TikTok."]] as const;

function money(value: number, currency: string) { return new Intl.NumberFormat("pt-BR", { style: "currency", currency, maximumFractionDigits: 2 }).format(value); }
function dateLabel(value?: string) { const date = value ? new Date(value) : null; return date && !Number.isNaN(date.getTime()) ? date.toLocaleString("pt-BR", { dateStyle: "short", timeStyle: "short" }) : "agora"; }

export default function Home() {
  const router = useRouter(); const [step, setStep] = useState(0);
  const [overview, setOverview] = useState<Overview>({ connected: false, businessCenters: [], advertisers: [], warnings: [] });
  const [details, setDetails] = useState<Detail>({ advertiser: null, pixels: [], identities: [], warnings: [] }); const [catalogs, setCatalogs] = useState<Choice[]>([]);
  const [bcId, setBcId] = useState(""); const [advertiserId, setAdvertiserId] = useState(""); const [catalogId, setCatalogId] = useState(""); const [pixelId, setPixelId] = useState(""); const [identityId, setIdentityId] = useState("");
  const [query, setQuery] = useState(""); const [adText, setAdText] = useState(""); const [cta, setCta] = useState("LEARN_MORE"); const [addon, setAddon] = useState("NONE"); const [proxy, setProxy] = useState("DIRECT");
  const [campaignName, setCampaignName] = useState(""); const [inputCurrency, setInputCurrency] = useState("MXN"); const [amount, setAmount] = useState("20000"); const [rate, setRate] = useState("0.30"); const [campaigns, setCampaigns] = useState("1"); const [groups, setGroups] = useState("1"); const [ads, setAds] = useState("1"); const [country, setCountry] = useState("MX"); const [language, setLanguage] = useState("es");
  const [loading, setLoading] = useState<"overview" | "assets" | "catalogs" | null>(null); const [notice, setNotice] = useState<Notice | null>(null); const [showConsole, setShowConsole] = useState(false);
  const selectedBc = overview.businessCenters.find((item) => item.id === bcId) ?? null; const selectedAdvertiser = overview.advertisers.find((item) => item.id === advertiserId) ?? details.advertiser; const selectedCatalog = catalogs.find((item) => item.id === catalogId) ?? null; const selectedPixel = details.pixels.find((item) => item.id === pixelId) ?? null; const selectedIdentity = details.identities.find((item) => item.id === identityId) ?? null; const accountCurrency = details.advertiser?.currency ?? selectedAdvertiser?.currency ?? "BRL";
  const counts = useMemo(() => { const campaignCount = Math.max(1, Number(campaigns) || 1); const groupCount = campaignCount * Math.max(1, Number(groups) || 1); return { campaigns: campaignCount, groups: groupCount, ads: groupCount * Math.max(1, Number(ads) || 1) }; }, [ads, campaigns, groups]);
  const budget = useMemo(() => { const value = Number(amount.replace(",", ".")) || 0; const fx = inputCurrency === accountCurrency ? 1 : Number(rate.replace(",", ".")) || 0; return value * fx; }, [accountCurrency, amount, inputCurrency, rate]);
  const ready = Boolean(bcId && advertiserId && catalogId && pixelId && identityId && adText.trim() && campaignName.trim()); const warnings = overview.warnings.concat(details.warnings); const filteredAdvertisers = overview.advertisers.filter((item) => (item.name + " " + item.id).toLowerCase().includes(query.toLowerCase()));
  const loadOverview = useCallback(async () => { setLoading("overview"); try { const response = await fetch("/api/tiktok/assets?scope=overview", { cache: "no-store" }); const payload = await response.json() as Overview & { error?: string }; if (!response.ok) throw new Error(payload.error || "Não foi possível consultar o TikTok."); setOverview(payload); if (!payload.connected) setNotice({ tone: "warning", text: "Conecte o TikTok para carregar ativos reais desta sessão." }); else { setBcId((current) => current || payload.businessCenters[0]?.id || ""); setAdvertiserId((current) => current || payload.advertisers[0]?.id || ""); setNotice({ tone: "success", text: `${payload.businessCenters.length} Business Center(s) e ${payload.advertisers.length} conta(s) carregados.` }); } } catch (error) { setNotice({ tone: "error", text: error instanceof Error ? error.message : "Falha ao carregar os ativos." }); } finally { setLoading(null); } }, []);
  const loadAssets = useCallback(async (id: string) => { if (!id || !overview.connected) return; setLoading("assets"); try { const response = await fetch(`/api/tiktok/assets?scope=advertiser&advertiser_id=${encodeURIComponent(id)}`, { cache: "no-store" }); const payload = await response.json() as Detail & { error?: string }; if (!response.ok) throw new Error(payload.error || "Não foi possível carregar os ativos da conta."); setDetails(payload); setPixelId((current) => current || payload.pixels[0]?.id || ""); setIdentityId((current) => current || payload.identities[0]?.id || ""); } catch (error) { setDetails({ advertiser: null, pixels: [], identities: [], warnings: [error instanceof Error ? error.message : "Falha ao carregar os ativos."] }); } finally { setLoading(null); } }, [overview.connected]);
  const loadCatalogs = useCallback(async (id: string) => { if (!id || !overview.connected) return; setLoading("catalogs"); try { const response = await fetch(`/api/tiktok/assets?scope=catalogs&bc_id=${encodeURIComponent(id)}`, { cache: "no-store" }); const payload = await response.json() as { catalogs?: Choice[]; error?: string }; if (!response.ok) throw new Error(payload.error || "Não foi possível carregar os catálogos."); const list = payload.catalogs ?? []; setCatalogs(list); setCatalogId((current) => current || list[0]?.id || ""); } catch (error) { setCatalogs([]); setNotice({ tone: "warning", text: error instanceof Error ? error.message : "O TikTok não retornou catálogos." }); } finally { setLoading(null); } }, [overview.connected]);
  useEffect(() => { if (new URLSearchParams(window.location.search).get("tiktok") === "connected") window.history.replaceState({}, "", window.location.pathname); const timer = window.setTimeout(() => void loadOverview(), 0); return () => window.clearTimeout(timer); }, [loadOverview]);
  useEffect(() => { if (!advertiserId) return; const timer = window.setTimeout(() => void loadAssets(advertiserId), 0); return () => window.clearTimeout(timer); }, [advertiserId, loadAssets]);
  useEffect(() => { if (!bcId) return; const timer = window.setTimeout(() => void loadCatalogs(bcId), 0); return () => window.clearTimeout(timer); }, [bcId, loadCatalogs]);
  const go = (next: number) => { setStep(next); window.scrollTo({ top: 0, behavior: "smooth" }); };
  const disabled = step === 6 ? !ready : false;
  return <main className="rocket-layout min-h-screen overflow-x-hidden bg-[#090d0f] text-zinc-100 selection:bg-[#ffd31a] selection:text-black"><div aria-hidden className="thor-storm fixed inset-0" /><div aria-hidden className="thor-scanlines pointer-events-none fixed inset-0" /><RocketHeader connected={overview.connected} bcs={overview.businessCenters.length} loading={loading === "overview"} onRefresh={loadOverview} /><div className="relative mx-auto flex min-h-[calc(100vh-60px)] max-w-[1720px] flex-col px-6 pb-5 pt-3 lg:px-10"><Journey step={step} onChange={go} />{notice && <NoticeBanner notice={notice} onClose={() => setNotice(null)} />}{warnings.length > 0 && <Warnings items={warnings} />}<section className="flex-1 pt-2"><StageTitle title={stageCopy[step][0]} subtitle={stageCopy[step][1]} />{step === 0 && <ConnectScreen connected={overview.connected} date={overview.authorizedAt} bcs={overview.businessCenters.length} advertisers={overview.advertisers.length} loading={loading === "overview"} onConnect={() => router.push("/api/tiktok/connect")} onRefresh={loadOverview} />}{step === 1 && <AccountsScreen advertisers={filteredAdvertisers} query={query} setQuery={setQuery} advertiserId={advertiserId} pixelId={pixelId} identityId={identityId} pixels={details.pixels} identities={details.identities} loading={loading === "assets"} onAdvertiser={(id) => { setAdvertiserId(id); setPixelId(""); setIdentityId(""); }} onPixel={setPixelId} onIdentity={setIdentityId} />}{step === 2 && <CatalogScreen bc={selectedBc} bcs={overview.businessCenters} catalogs={catalogs} selectedId={catalogId} loading={loading === "catalogs"} onBc={(id) => { setBcId(id); setCatalogId(""); }} onRefresh={() => void loadCatalogs(bcId)} onSelect={setCatalogId} />}{step === 3 && <CreativeScreen value={adText} cta={cta} addon={addon} onChange={setAdText} onCta={setCta} onAddon={setAddon} />}{step === 4 && <StructureScreen campaigns={campaigns} groups={groups} ads={ads} setCampaigns={setCampaigns} setGroups={setGroups} setAds={setAds} inputCurrency={inputCurrency} setInputCurrency={setInputCurrency} amount={amount} setAmount={setAmount} rate={rate} setRate={setRate} accountCurrency={accountCurrency} budget={budget} counts={counts} country={country} setCountry={setCountry} language={language} setLanguage={setLanguage} campaignName={campaignName} setCampaignName={setCampaignName} />}{step === 5 && <ProxyScreen proxy={proxy} onChange={setProxy} />}{step === 6 && <LaunchScreen ready={ready} name={campaignName} bc={selectedBc} advertiser={selectedAdvertiser} catalog={selectedCatalog} pixel={selectedPixel} identity={selectedIdentity} counts={counts} budget={budget} currency={accountCurrency} onOpen={() => setShowConsole(true)} />}</section><FooterNav step={step} onBack={() => go(Math.max(0, step - 1))} onNext={() => step === 6 ? setShowConsole(true) : go(step + 1)} disabled={disabled} /></div>{showConsole && <LaunchConsole name={campaignName} counts={counts} budget={budget} currency={accountCurrency} ready={ready} onClose={() => setShowConsole(false)} />}</main>;
}

function RocketHeader({ connected, bcs, loading, onRefresh }: { connected: boolean; bcs: number; loading: boolean; onRefresh: () => void }) { return <header className="relative z-20 h-[60px] border-b border-white/[.055] bg-[#070a0c]/90"><div className="mx-auto flex h-full max-w-[1720px] items-center justify-between px-6 lg:px-10"><div><p className="text-[22px] font-black leading-5 tracking-[-.06em] text-white">Thor<span className="text-[#ffd31a]">Tk</span></p><p className="mt-1 text-[9px] font-black uppercase tracking-[.2em] text-[#8ca6c7]">Criar campanhas</p></div><div className="flex items-center gap-2"><HeaderChip tone="blue" text={`BCs ${bcs}`} /><HeaderChip tone="red" text="GASTO HOJE 0,00" /><HeaderChip tone="green" text="SALDO --" /><button type="button" className="header-chip hidden md:flex"><KeyRound size={13} />PRESETS</button><button type="button" onClick={onRefresh} className="header-chip text-[#ffd31a]" title="Atualizar ativos"><RefreshCw size={14} className={loading ? "animate-spin" : ""} />{connected ? "CANAL ATIVO" : "CONECTAR"}</button></div></div></header>; }
function HeaderChip({ tone, text }: { tone: "blue" | "red" | "green"; text: string }) { const color = tone === "blue" ? "border-sky-400/30 text-sky-300" : tone === "red" ? "border-red-400/25 text-red-300" : "border-emerald-400/25 text-emerald-300"; return <span className={`header-chip hidden sm:flex ${color}`}>{text}</span>; }
function Journey({ step, onChange }: { step: number; onChange: (step: number) => void }) { return <nav aria-label="Etapas do lançamento" className="relative -mx-2 overflow-x-auto pb-2 pt-3 [scrollbar-width:none]"><div className="relative flex min-w-[930px] items-start justify-between px-4"><div aria-hidden className="absolute left-[7%] right-[7%] top-7 border-t-2 border-dashed border-[#4c5459]/55" />{steps.map((label, index) => { const active = index === step; return <button key={label} type="button" onClick={() => onChange(index)} className="relative z-10 flex w-[112px] flex-col items-center gap-2"><span className={`journey-icon ${active ? "journey-active" : ""}`}><Image src={`/thor-nav-${index}.png`} alt="" width={52} height={52} priority={index < 2} /></span><span className={`text-[10px] font-black tracking-[.07em] ${active ? "text-[#55a8ff]" : "text-zinc-500"}`}>{label}</span></button>; })}</div></nav>; }
function StageTitle({ title, subtitle }: { title: string; subtitle: string }) { return <div className="pt-1"><h1 className="text-[29px] font-black tracking-[-.055em] text-zinc-100">{title}</h1><p className="mt-1 text-[15px] text-zinc-400">{subtitle}</p></div>; }
function NoticeBanner({ notice, onClose }: { notice: Notice; onClose: () => void }) { const style = notice.tone === "success" ? "border-emerald-400/25 bg-emerald-400/[.07] text-emerald-100" : notice.tone === "error" ? "border-red-400/25 bg-red-400/[.08] text-red-100" : "border-amber-400/25 bg-amber-400/[.08] text-amber-100"; return <div className={`mt-3 flex items-center justify-between gap-4 rounded-lg border px-4 py-3 text-xs ${style}`}><span className="flex items-center gap-2">{notice.tone === "success" ? <CircleCheck size={15} /> : <CircleAlert size={15} />}{notice.text}</span><button type="button" onClick={onClose}><X size={15} /></button></div>; }
function Warnings({ items }: { items: string[] }) { return <div className="mt-3 rounded-lg border border-amber-400/20 bg-[#211d0b]/65 px-4 py-3 text-xs text-amber-100"><p className="font-black uppercase tracking-[.12em]">Retorno parcial do TikTok</p><ul className="mt-2 list-disc space-y-1 pl-4">{items.map((item, index) => <li key={`${item}-${index}`}>{item}</li>)}</ul></div>; }

function ConnectScreen({ connected, date, bcs, advertisers, loading, onConnect, onRefresh }: { connected: boolean; date?: string; bcs: number; advertisers: number; loading: boolean; onConnect: () => void; onRefresh: () => void }) { return <div className="mx-auto max-w-[510px] pt-7 text-center"><div className="thor-crest"><Image src="/thor-nav-0.png" alt="Mjölnir energizado" width={112} height={112} priority /></div><h2 className="mt-5 text-[36px] font-black tracking-[-.06em] text-zinc-100">{connected ? "Força Pronta" : "Canal Inativo"}</h2><p className="mx-auto mt-3 max-w-md text-sm leading-6 text-zinc-400">{connected ? `${bcs} Business Center(s) e ${advertisers} conta(s) conectados. A tempestade está sob controle.` : "Autorize o TikTok para carregar seus ativos reais e preparar a estrutura ABO."}</p><div className="mt-7 space-y-3 text-left"><div className="rocket-card border-[#ffd31a]/50 bg-[#151508]/80 p-5"><div className="flex items-center gap-4"><span className="grid h-14 w-14 place-items-center overflow-hidden rounded-xl bg-[#121519] shadow-[0_0_24px_rgba(255,211,26,.35)]"><Image src="/thor-nav-6.png" alt="Martelo de Thor" width={56} height={56} /></span><div><div className="flex items-center gap-2"><p className="font-black">Comando de Thor</p><span className="rounded bg-[#5b4a0b] px-2 py-0.5 text-[9px] font-black uppercase tracking-wide text-[#ffd31a]">modo ABO</span></div><p className="mt-1 text-xs leading-5 text-zinc-400">Controle manual dos grupos, orçamento e ativos autorizados.</p></div></div></div>{connected ? <div className="rocket-card border-emerald-400/35 bg-emerald-400/[.07] px-5 py-4"><div className="flex items-center gap-3"><span className="h-2 w-2 rounded-full bg-emerald-400 shadow-[0_0_13px_#34d399]" /><div className="min-w-0 flex-1"><p className="truncate text-sm font-black text-zinc-100">Canal TikTok autorizado</p><p className="mt-1 text-[11px] text-zinc-500">Última autorização: {dateLabel(date)}</p></div><button type="button" onClick={onRefresh} disabled={loading} className="text-sky-300 hover:text-sky-200"><RefreshCw size={17} className={loading ? "animate-spin" : ""} /></button></div></div> : <button type="button" onClick={onConnect} className="rocket-outline-action"><PlugZap size={17} />Conectar TikTok Business</button>}<div className="rocket-outline-action cursor-default text-sky-300"><ShieldCheck size={17} />Tokens protegidos no servidor</div></div></div>; }

function AccountsScreen({ advertisers, query, setQuery, advertiserId, pixelId, identityId, pixels, identities, loading, onAdvertiser, onPixel, onIdentity }: { advertisers: Choice[]; query: string; setQuery: (v: string) => void; advertiserId: string; pixelId: string; identityId: string; pixels: Choice[]; identities: Choice[]; loading: boolean; onAdvertiser: (v: string) => void; onPixel: (v: string) => void; onIdentity: (v: string) => void }) { return <div className="pt-5"><div className="flex gap-3"><label className="relative flex-1"><Search className="pointer-events-none absolute left-4 top-1/2 -translate-y-1/2 text-zinc-500" size={18} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Buscar por nome, ID ou status..." className="rocket-input pl-11" /></label><button type="button" className="rocket-dark-button"><Layers3 size={16} />Filtro</button></div><div className="mt-3 overflow-hidden rounded-lg border border-white/[.075] bg-[#121719]/95"><div className="grid grid-cols-[minmax(380px,1fr)_minmax(180px,.5fr)_160px] gap-4 bg-[#1d222b] px-4 py-3 text-[11px] font-black text-zinc-400"><span>Nome da Conta</span><span>ID Externo</span><span>Status</span></div><div className="max-h-[285px] overflow-y-auto">{advertisers.length ? advertisers.map((item) => <button key={item.id} type="button" onClick={() => onAdvertiser(item.id)} className={`grid w-full grid-cols-[minmax(380px,1fr)_minmax(180px,.5fr)_160px] items-center gap-4 border-t border-white/[.06] px-4 py-3 text-left text-xs transition ${item.id === advertiserId ? "bg-[#24210d]/35" : "hover:bg-white/[.03]"}`}><span className="flex items-center gap-3"><span className={`grid h-4 w-4 place-items-center rounded border ${item.id === advertiserId ? "border-[#ffd31a] bg-[#ffd31a] text-black" : "border-zinc-600"}`}>{item.id === advertiserId && <Check size={12} strokeWidth={3} />}</span><span className="truncate font-bold text-zinc-100">{item.name}</span></span><span className="truncate text-sky-300">{item.id}</span><span><b className="rounded border border-emerald-400/20 bg-emerald-400/[.1] px-2 py-1 text-[10px] text-emerald-300">● Ativa</b></span></button>) : <div className="p-10 text-center text-sm text-zinc-500">Nenhuma conta foi retornada.</div>}</div><div className="flex items-center justify-between bg-[#1d222b] px-4 py-3 text-xs"><b className="text-[#ffd31a]">{advertiserId ? "1 CONTA SELECIONADA" : "0 CONTAS SELECIONADAS"}</b><button type="button" onClick={() => onAdvertiser("")} className="text-zinc-100 hover:text-white">Cancelar seleção</button></div></div><div className="mt-7 grid gap-6 lg:grid-cols-2"><AssetAction icon={<UserRound size={22} />} title="Criar Identity" description="Defina um perfil personalizado para as contas selecionadas."><AssetSelect label="Identidade do anúncio" values={identities} selected={identityId} loading={loading} onChange={onIdentity} empty="Selecione uma conta para carregar identidades." /></AssetAction><AssetAction icon={<Orbit size={22} />} title="Vincular Pixel" description="Conecte pixels de rastreamento para capturar dados de conversão."><AssetSelect label="Pixel de conversão" values={pixels} selected={pixelId} loading={loading} onChange={onPixel} empty="Selecione uma conta para carregar pixels." /></AssetAction></div></div>; }
function AssetAction({ icon, title, description, children }: { icon: React.ReactNode; title: string; description: string; children: React.ReactNode }) { return <div className="rocket-card min-h-[170px] p-5"><div className="flex items-center gap-4"><span className="grid h-12 w-12 place-items-center rounded-xl bg-[#2b2616] text-[#ffd31a]">{icon}</span><div><h2 className="text-xl font-black tracking-[-.04em]">{title}</h2><p className="mt-1 text-sm text-zinc-400">{description}</p></div></div><div className="mt-5">{children}</div></div>; }
function AssetSelect({ label, values, selected, loading, onChange, empty }: { label: string; values: Choice[]; selected: string; loading: boolean; onChange: (v: string) => void; empty: string }) { return <label className="block text-[10px] font-black uppercase tracking-[.1em] text-zinc-500">{label}{loading ? <span className="mt-2 flex items-center gap-2 text-xs normal-case text-zinc-400"><Loader2 className="animate-spin" size={14} />Consultando TikTok…</span> : values.length ? <span className="relative mt-2 block"><select value={selected} onChange={(event) => onChange(event.target.value)} className="rocket-input appearance-none text-sm font-bold"><option value="">Selecione</option>{values.map((item) => <option key={item.id} value={item.id}>{item.name} · {item.id}</option>)}</select><ChevronDown className="pointer-events-none absolute right-3 top-1/2 -translate-y-1/2 text-zinc-500" size={15} /></span> : <span className="mt-2 block normal-case text-xs font-medium text-zinc-500">{empty}</span>}</label>; }

function CatalogScreen({ bc, bcs, catalogs, selectedId, loading, onBc, onRefresh, onSelect }: { bc: Choice | null; bcs: Choice[]; catalogs: Choice[]; selectedId: string; loading: boolean; onBc: (id: string) => void; onRefresh: () => void; onSelect: (id: string) => void }) { return <div className="pt-5"><div className="flex items-center justify-between"><p className="text-sm text-zinc-400">Selecione um catálogo por BC. Cada conta usará o catálogo do seu Business Center.</p><button type="button" onClick={onRefresh} className="text-xs font-bold text-sky-300"><RefreshCw className={`mr-1 inline ${loading ? "animate-spin" : ""}`} size={14} />Atualizar</button></div><div className="mt-5 rounded-xl border border-[#6b5b16]/65 bg-[#1a1a12]/80 p-6"><div className="flex items-center gap-2 text-sm font-black text-zinc-100"><span className="h-2.5 w-2.5 rounded-full bg-[#ffd31a]" />{bc?.name ?? "Selecione um Business Center"}<span className="ml-2 rounded bg-zinc-800 px-2 py-1 text-[10px] text-zinc-400">{catalogs.length} catálogo(s)</span></div><div className="mt-5 flex flex-wrap gap-3">{bcs.map((item) => <button key={item.id} type="button" onClick={() => onBc(item.id)} className={`rounded-xl border px-5 py-4 text-left transition ${item.id === bc?.id ? "border-[#ffd31a] bg-[#312b10]" : "border-white/[.09] bg-[#101417] hover:border-white/[.24]"}`}><p className="font-black text-zinc-100">{item.name}</p><p className="mt-1 text-xs text-zinc-500">ID: {item.id}</p></button>)}</div></div><div className="mt-5 grid gap-4 md:grid-cols-2"><SelectMode icon={<Layers3 size={28} />} title="Selecionar Sets" text="Segmente grupos específicos do catálogo." /><SelectMode icon={<Database size={28} />} title="Todos os Produtos" text="Promova todos os produtos do catálogo." active /></div><div className="mt-5 grid gap-3 md:grid-cols-2">{loading ? <div className="py-10 text-center text-sm text-zinc-500"><Loader2 className="mx-auto animate-spin text-[#ffd31a]" />Carregando catálogos…</div> : catalogs.map((item) => <button key={item.id} type="button" onClick={() => onSelect(item.id)} className={`catalog-card text-left ${item.id === selectedId ? "catalog-selected" : ""}`}><span className="grid h-9 w-9 place-items-center rounded-lg bg-white/[.06] text-[#ffd31a]"><Database size={17} /></span><p className="mt-4 font-black">{item.name}</p><p className="mt-1 text-xs text-zinc-500">ID: {item.id} · {item.currency || "Moeda da conta"}</p></button>)}</div></div>; }
function SelectMode({ icon, title, text, active = false }: { icon: React.ReactNode; title: string; text: string; active?: boolean }) { return <button type="button" className={`flex min-h-[155px] flex-col justify-between rounded-xl border p-6 text-left transition ${active ? "border-[#ffd31a] bg-[#251f0d]" : "border-white/[.07] bg-[#101417] hover:border-white/[.2]"}`}><span className={active ? "text-[#ffd31a]" : "text-zinc-400"}>{icon}</span><div><p className="text-xl font-black">{title}</p><p className="mt-2 text-sm text-zinc-400">{text}</p></div></button>; }

function CreativeScreen({ value, cta, addon, onChange, onCta, onAddon }: { value: string; cta: string; addon: string; onChange: (v: string) => void; onCta: (v: string) => void; onAddon: (v: string) => void }) { const calls = [["SHOP_NOW", "Compre Agora"], ["LEARN_MORE", "Saiba Mais"], ["SIGN_UP", "Cadastre-se"], ["CONTACT_US", "Contato"], ["DOWNLOAD", "Download"], ["WATCH_NOW", "Ver Agora"], ["RESERVE_NOW", "Reservar"], ["APPLY_NOW", "Aplicar"]]; const addOns = [["NONE", "Nenhum", X], ["DISPLAY", "Display card", Layers3], ["COUNTDOWN", "Countdown", Timer], ["GIFT", "Gift code", Gift]] as const; return <div className="mx-auto max-w-[565px] pt-4"><div className="rocket-section"><div className="flex items-center justify-between"><p className="section-label">Texto do anúncio</p><span className="flex items-center gap-2 text-xs text-zinc-400">Puxar do catálogo <span className="h-5 w-9 rounded-full bg-zinc-600 p-0.5"><span className="block h-4 w-4 rounded-full bg-zinc-300" /></span></span></div><textarea value={value} onChange={(event) => onChange(event.target.value)} placeholder="Digite aqui o texto do anúncio" className="rocket-input mt-3 min-h-[84px] resize-y" /></div><div className="rocket-section mt-3"><p className="section-label">Call to action</p><div className="mt-3 flex flex-wrap gap-2">{calls.map(([key, label]) => <button type="button" key={key} onClick={() => onCta(key)} className={`cta-chip ${cta === key ? "cta-selected" : ""}`}>{label}</button>)}</div></div><div className="rocket-section mt-3"><p className="section-label">Complemento interativo</p><div className="mt-3 grid grid-cols-4 gap-3">{addOns.map(([key, label, Icon]) => <button type="button" key={key} onClick={() => onAddon(key)} className={`addon-card ${addon === key ? "addon-selected" : ""}`}><Icon size={27} /><span>{label}</span></button>)}</div></div></div>; }

function StructureScreen({ campaigns, groups, ads, setCampaigns, setGroups, setAds, inputCurrency, setInputCurrency, amount, setAmount, rate, setRate, accountCurrency, budget, counts, country, setCountry, language, setLanguage, campaignName, setCampaignName }: { campaigns: string; groups: string; ads: string; setCampaigns: (v: string) => void; setGroups: (v: string) => void; setAds: (v: string) => void; inputCurrency: string; setInputCurrency: (v: string) => void; amount: string; setAmount: (v: string) => void; rate: string; setRate: (v: string) => void; accountCurrency: string; budget: number; counts: { campaigns: number; groups: number; ads: number }; country: string; setCountry: (v: string) => void; language: string; setLanguage: (v: string) => void; campaignName: string; setCampaignName: (v: string) => void }) { return <div className="mx-auto max-w-[1210px] pt-5"><div className="structure-grid"><MetricInput label="Campanhas" value={campaigns} onChange={setCampaigns} /><MetricInput label="Grupos" value={groups} onChange={setGroups} /><MetricInput label="Anúncios" value={ads} onChange={setAds} /><label className="structure-metric"><span>Orçamento ({inputCurrency})</span><div className="flex items-center gap-2"><input value={amount} onChange={(event) => setAmount(event.target.value)} className="w-full bg-transparent text-2xl font-black outline-none" /><select value={inputCurrency} onChange={(event) => setInputCurrency(event.target.value)} className="bg-transparent text-sm font-black outline-none">{currencies.map((item) => <option key={item}>{item}</option>)}</select></div></label></div><div className="rocket-section mt-5"><div className="flex items-center justify-between"><p className="section-label">Conteúdo gerado por IA (AIGC)</p><ToggleOff /></div><p className="mt-3 text-xs text-zinc-500">Ative somente quando o criativo for realmente gerado por IA.</p></div><div className="rocket-section mt-4"><div className="flex items-center justify-between"><p className="section-label">Lance (CPA)</p><ToggleOff /></div><p className="mt-3 text-xs text-zinc-500">Base no custo por aquisição desejado.</p></div><div className="rocket-section mt-4"><p className="section-label">Janela de atribuição</p><div className="mt-4 grid gap-4 md:grid-cols-3"><DarkSelect label="Click" value="7-day click" values={["7-day click", "1-day click"]} onChange={() => undefined} /><DarkSelect label="View" value="1-day view" values={["1-day view", "None"]} onChange={() => undefined} /><DarkSelect label="Contagem" value="Every" values={["Every", "Once"]} onChange={() => undefined} /></div></div><div className="mt-4 grid gap-4 md:grid-cols-[1.2fr_.8fr]"><div className="rocket-section"><div className="flex items-center justify-between"><p className="section-label">Faixas etárias</p><button type="button" className="small-yellow">Todas</button></div><div className="mt-4 flex flex-wrap gap-2">{["13–17", "18–24", "25–34", "35–44", "45–54", "55+"].map((item, index) => <button type="button" key={item} className={`age-chip ${index ? "age-selected" : ""}`}>{item}</button>)}</div><p className="mt-3 text-xs text-zinc-500">5 faixa(s) selecionada(s)</p></div><div className="rocket-section"><p className="section-label">Sistema operacional</p><div className="mt-4 grid gap-2"><button type="button" className="os-card">Android</button><button type="button" className="os-card">iOS</button><button type="button" className="os-card os-selected">Android + iOS</button></div></div></div><div className="mt-4 grid gap-4 md:grid-cols-2"><DarkSelect label="País (location)" value={country} values={["MX", "BR", "US", "CL"]} onChange={setCountry} /><DarkSelect label="Idioma" value={language} values={["es", "pt", "en"]} onChange={setLanguage} /></div><div className="rocket-section mt-4"><p className="section-label">Estados / regiões (opcional)</p><button type="button" className="rocket-dark-button mt-4">Carregar estados de {country === "MX" ? "México" : country}</button><p className="mt-3 text-xs text-zinc-500">Vazio = país inteiro. Estados selecionados substituem o país no targeting.</p></div><div className="rocket-section mt-4"><p className="section-label">Campaign naming <span className="ml-2 rounded bg-[#3a3319] px-2 py-1 text-[9px] text-[#ffd31a]">nomenclatura</span></p><div className="mt-5 grid gap-5 md:grid-cols-[1.5fr_.65fr]"><label className="text-xs font-bold text-zinc-400">Nome da campanha<input value={campaignName} onChange={(event) => setCampaignName(event.target.value)} placeholder="Ex.: P268MXN-TEST" className="rocket-input mt-2 font-black text-white" /></label><div className="rounded-xl bg-[#101417] p-4 text-center"><p className="text-[10px] font-black uppercase tracking-[.1em] text-zinc-500">Prévia do nome</p><p className="mt-3 rounded-lg border border-[#6b5b16] bg-[#29230e] px-4 py-3 text-lg font-black text-[#ffd31a]">{campaignName ? `${campaignName} 01.` : "—"}</p></div></div></div><div className="mt-4 rounded-xl border border-[#ffd31a]/25 bg-[#ffd31a]/[.06] p-5"><p className="section-label text-[#ffd31a]">Impacto do orçamento</p><p className="mt-2 text-2xl font-black">{money(budget, accountCurrency)} <span className="text-sm font-bold text-zinc-400">por grupo · {counts.groups} grupos · teto diário {money(budget * counts.groups, accountCurrency)}</span></p>{inputCurrency !== accountCurrency && <label className="mt-4 block text-xs text-zinc-400">Cotação: 1 {inputCurrency} em {accountCurrency}<input value={rate} onChange={(event) => setRate(event.target.value)} className="rocket-input mt-2 max-w-xs" /></label>}</div></div>; }
function MetricInput({ label, value, onChange }: { label: string; value: string; onChange: (v: string) => void }) { return <label className="structure-metric"><span>{label}</span><input type="number" min="1" value={value} onChange={(event) => onChange(event.target.value)} /></label>; }
function DarkSelect({ label, value, values, onChange }: { label: string; value: string; values: string[]; onChange: (v: string) => void }) { return <label className="rocket-section block"><span className="section-label">{label}</span><span className="relative mt-4 block"><select value={value} onChange={(event) => onChange(event.target.value)} className="rocket-input appearance-none font-black">{values.map((item) => <option key={item} value={item}>{item}</option>)}</select><ChevronDown className="pointer-events-none absolute right-3 top-1/2 -translate-y-1/2 text-zinc-500" size={16} /></span></label>; }
function ToggleOff() { return <span className="flex overflow-hidden rounded-md border border-white/[.08] bg-[#0d1013] text-xs font-black"><b className="bg-zinc-700 px-5 py-3 text-zinc-300">Off</b><b className="px-5 py-3 text-zinc-500">On</b></span>; }

function ProxyScreen({ proxy, onChange }: { proxy: string; onChange: (value: string) => void }) { return <div className="mx-auto max-w-[700px] pt-7"><div className="rocket-card p-7"><div className="flex items-center gap-4"><span className="grid h-14 w-14 place-items-center rounded-xl bg-[#142231] text-sky-300"><Orbit size={27} /></span><div><p className="text-xl font-black">Canal de saída</p><p className="mt-1 text-sm text-zinc-400">A API receberá esta configuração junto com cada conta selecionada.</p></div></div><div className="mt-7 grid gap-3"><button type="button" onClick={() => onChange("DIRECT")} className={`proxy-card ${proxy === "DIRECT" ? "proxy-selected" : ""}`}><span><b>Rota direta</b><small>Usar a conexão padrão da operação.</small></span>{proxy === "DIRECT" && <CircleCheck />}</button><button type="button" onClick={() => onChange("DEDICATED")} className={`proxy-card ${proxy === "DEDICATED" ? "proxy-selected" : ""}`}><span><b>Proxy dedicado</b><small>Informar um proxy separado por conta antes do lançamento.</small></span>{proxy === "DEDICATED" && <CircleCheck />}</button></div></div></div>; }
function LaunchScreen({ ready, name, bc, advertiser, catalog, pixel, identity, counts, budget, currency, onOpen }: { ready: boolean; name: string; bc: Choice | null; advertiser: Choice | null; catalog: Choice | null; pixel: Choice | null; identity: Choice | null; counts: { campaigns: number; groups: number; ads: number }; budget: number; currency: string; onOpen: () => void }) { const rows = [["Business Center", bc], ["Conta", advertiser], ["Catálogo", catalog], ["Pixel", pixel], ["Identity", identity]] as const; return <div className="mx-auto max-w-[820px] pt-6"><div className="rocket-card overflow-hidden"><div className="border-b border-white/[.08] px-6 py-5"><p className="section-label">Resumo de lançamento</p><h2 className="mt-2 text-3xl font-black">{name || "Nome da campanha pendente"}</h2></div><div className="p-6"><div className="divide-y divide-white/[.07]">{rows.map(([label, item]) => <div key={label} className="flex items-center justify-between gap-4 py-3 text-sm"><span className="text-zinc-500">{label}</span><span className={`flex min-w-0 items-center gap-2 text-right font-bold ${item ? "text-emerald-300" : "text-amber-300"}`}>{item ? <CircleCheck size={15} /> : <CircleAlert size={15} />}<span className="truncate">{item?.name ?? "Pendente"}</span></span></div>)}</div><div className="mt-5 rounded-lg border border-[#ffd31a]/35 bg-[#2b250e] p-4"><p className="text-xs text-[#f6df73]">{counts.campaigns} campanha(s) · {counts.groups} grupo(s) · {counts.ads} anúncio(s)</p><p className="mt-2 text-xl font-black">Teto diário: {money(budget * counts.groups, currency)}</p></div><button type="button" disabled={!ready} onClick={onOpen} className="rocket-launch-button mt-6 disabled:opacity-40"><CloudLightning size={19} />Preparar console de lançamento</button></div></div></div>; }
function FooterNav({ step, onBack, onNext, disabled }: { step: number; onBack: () => void; onNext: () => void; disabled: boolean }) { return <footer className="mt-7 flex items-center justify-between"><button type="button" onClick={onBack} className="footer-abort"><ArrowLeft size={16} />{step === 0 ? "Abortar missão" : "Anterior"}</button><button type="button" onClick={onNext} disabled={disabled} className="footer-next disabled:cursor-not-allowed disabled:opacity-40">{step === 6 ? "Preparar lançamento" : "Próxima fase"}<ArrowRight size={17} /></button></footer>; }
function LaunchConsole({ name, counts, budget, currency, ready, onClose }: { name: string; counts: { campaigns: number; groups: number; ads: number }; budget: number; currency: string; ready: boolean; onClose: () => void }) { const logs = [["!", `Conta usa ${currency}; orçamento calculado em ${currency}: ${money(budget, currency)} por grupo.`, "warning"], ["!", `Teto diário estimado: ${money(budget * counts.groups, currency)}.`, "warning"], ["▶", `Campanha: ${name || "sem nome"}`, "info"], ["⚡", `Estrutura: ${counts.campaigns} campanha(s) | ${counts.groups} grupo(s) | ${counts.ads} anúncio(s)`, "info"], ["✓", "Pré-flight concluído. Nenhuma campanha foi criada nesta tela.", ready ? "success" : "error"]] as const; return <div className="fixed inset-0 z-50 grid place-items-center bg-black/80 p-4 backdrop-blur-sm"><div role="dialog" aria-modal="true" className="console-modal"><div className="flex items-center justify-between border-b border-white/[.08] px-6 py-5"><div className="flex items-center gap-3"><span className="console-orb" /><h2 className="text-xl font-black">Preparando campanhas...</h2></div><div className="flex gap-2"><span className="rounded bg-[#4b4116] px-2 py-1 text-[10px] font-black text-[#ffd31a]">2 avisos</span><button type="button" onClick={onClose} className="rounded border border-red-400/40 px-3 py-1 text-[10px] font-black text-red-300">PARAR</button></div></div><div className="p-6"><div className="flex items-center justify-between border-b border-white/[.06] pb-3 text-xs"><div className="flex gap-5 text-zinc-400"><b className="rounded bg-zinc-700 px-3 py-2 text-zinc-100">Todos (5)</b><span>Erros (0)</span><span>Avisos (2)</span><span>Sucesso (1)</span></div><span className="text-sky-300">Copiar · Exportar</span></div><label className="relative mt-4 block"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-zinc-600" size={14} /><input className="rocket-input h-9 pl-9 text-xs" placeholder="Buscar nos logs..." /></label><div className="mt-5 h-[245px] space-y-3 overflow-y-auto font-mono text-xs leading-5">{logs.map(([mark, text, tone], index) => <p key={index} className={tone === "success" ? "text-emerald-300" : tone === "warning" ? "text-[#ffd31a]" : tone === "error" ? "text-red-300" : "text-sky-300"}><span className="mr-3 opacity-60">[{new Date().toLocaleTimeString("pt-BR", { hour12: false })}]</span><b className="mr-2">{mark}</b>{text}</p>)}</div><div className="mt-5 border-t border-white/[.08] pt-4"><p className="font-black">Resumo: <span className="text-emerald-300">1 sucesso</span> · <span className="text-[#ffd31a]">2 avisos</span></p><p className="mt-4 text-[10px] font-black uppercase tracking-[.13em] text-zinc-500">A criação real será habilitada após integrar o endpoint de publicação do TikTok.</p></div></div><div className="flex justify-center border-t border-white/[.08] px-6 py-5"><button type="button" onClick={onClose} className="console-close">Fechar</button></div></div></div>; }
